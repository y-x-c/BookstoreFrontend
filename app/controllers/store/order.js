import Ember from 'ember';

export default Ember.Controller.extend({

  //totalPrice: Ember.computed('model.orderItems.@each', function(){
  //  var items = this.get('model.orderItems');
  //  var totalPrice = 0;
  //
  //  for(var i = 0; i < items.get('length'); i++) {
  //    var item = items.objectAt(i);
  //    totalPrice += item.get('price') * item.get('amount');
  //  }
  //
  //  return totalPrice;
  //})
});
