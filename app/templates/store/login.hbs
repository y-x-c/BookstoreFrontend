<div class="container-fluid">
    <div class="col-sm-5 col-xs-12 pull-right">
        <div id="loginPanel" class="panel panel-default">
            <!--<div class="panel-heading">-->
            <!--<h3 class="panel-title">Log in</h3>-->
            <!--</div>-->
            <div class="panel-body">
                <form>
                    <div {{bind-attr class=":form-group errorUsr:has-feedback errorUsr:has-error"}} >
                        <label for="inputUsername" class="control-label">Username</label>
                      {{input type="text" class="form-control" id="inputUsername" placeholder="Username" value=username}}
                      {{#if errorUsr}}
                          <span class="glyphicon glyphicon-remove form-control-feedback" aria-hidden="true"></span>
                          <span id="inputUsernameStatus" class="sr-only">(failed)</span>
                        {{#if username}}
                        {{else}}
                            <span id="helpBlock" class="help-block">Empty username is not allowed!</span>
                        {{/if}}
                      {{/if}}
                    </div>
                    <div {{bind-attr class=":form-group errorPwd:has-feedback errorPwd:has-error"}} >
                        <label for="inputPassword" class="control-label">Password</label>
                      {{input type="password" class="form-control" id="inputPassword" placeholder="Password" value=password}}
                      {{#if errorPwd}}
                          <span class="glyphicon glyphicon-remove form-control-feedback" aria-hidden="true"></span>
                          <span id="inputUsernameStatus" class="sr-only">(failed)</span>
                        {{#if password}}
                        {{else}}
                            <span id="helpBlock" class="help-block">Empty password is not allowed!</span>
                        {{/if}}
                      {{/if}}
                    </div>
                    <div class="form-group">
                        <div class="pull-right">
                            <button {{action 'login' username password}} class="btn btn-primary"
                              {{bind-attr disabled=invalid}}>Login</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>